# Legacy HTML sources

These files are the **only** HTML sources used by `npm run prepare:legacy` to generate `content/bodies/*.html` and `content/page-config.json` for the Next app.

Edit pages here (or in React later), not in the parent repo root. **Images, CSS, and root `*.js`** still live one level up (`../`) and are copied by the same prepare script.
